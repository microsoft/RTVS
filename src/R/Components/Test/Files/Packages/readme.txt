This folder contains the sources for the packages that are published to the repositories under the Repos folder.

After rebuilding a package:
 - copy the .gz file to the repository's src/contrib folder.
 - copy the .zip file to the repository's bin/windows/contrib/x.y folder.
 - update the PACKAGES files as needed
