<ui:DialogWindow x:Class="Microsoft.VisualStudio.R.Package.DataInspect.DataImport.ImportDataWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Microsoft.VisualStudio.R.Package.DataInspect.DataImport"
        xmlns:ui="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.14.0"
        mc:Ignorable="d"
        Title="ImportDataWindow" Height="300" Width="300">
    <ui:DialogWindow.Resources>
        <DataTemplate x:Key="RStringTemplate">
            <TextBlock Text="{Binding Name}" />
        </DataTemplate>
    </ui:DialogWindow.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                
                <!-- FilePath -->
                <TextBlock Grid.Column="0" Grid.Row="0">File</TextBlock>
                <Grid Grid.Column="1" Grid.Row="0"
                        x:Name="ChooseFile">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" x:Name="FilePathBox" />
                    <Button Grid.Column="1" x:Name="FileOpenButton" Content="..." Click="FileOpenButton_Click" MinWidth="50"/>
                </Grid>
                <!-- Name -->
                <TextBlock Grid.Column="0" Grid.Row="1">Name</TextBlock>
                <TextBox Grid.Column="1" Grid.Row="1"
                         x:Name="VariableNameBox" />
                <!-- Encoding -->
                <TextBlock Grid.Column="0" Grid.Row="2">Encoding</TextBlock>
                <ComboBox Grid.Column="1" Grid.Row="2" x:Name="EncodingComboBox" ItemTemplate="{StaticResource RStringTemplate}" SelectionChanged="ComboBox_SelectionChanged" />
                <!-- Header -->
                <TextBlock Grid.Column="0" Grid.Row="3">Header</TextBlock>
                <CheckBox Grid.Column="1" Grid.Row="3" x:Name="HeaderCheckBox"  IsChecked="True" Unchecked="HeaderCheckBox_Changed" Checked="HeaderCheckBox_Changed"/>
                <!-- Row names -->
                <TextBlock Grid.Column="0" Grid.Row="4">Row names</TextBlock>
                <ComboBox Grid.Column="1" Grid.Row="4" x:Name="RowNamesComboBox" ItemTemplate="{StaticResource RStringTemplate}" SelectionChanged="ComboBox_SelectionChanged" />
                <!-- Separator -->
                <TextBlock Grid.Column="0" Grid.Row="5">Separator</TextBlock>
                <ComboBox Grid.Column="1" Grid.Row="5" x:Name="SeparatorComboBox" ItemTemplate="{StaticResource RStringTemplate}" SelectionChanged="ComboBox_SelectionChanged" />
                <!-- Decimal -->
                <TextBlock Grid.Column="0" Grid.Row="6">Decimal</TextBlock>
                <ComboBox Grid.Column="1" Grid.Row="6" x:Name="DecimalComboBox" ItemTemplate="{StaticResource RStringTemplate}" SelectionChanged="ComboBox_SelectionChanged" />
                <!-- Quote -->
                <TextBlock Grid.Column="0" Grid.Row="7">Quote</TextBlock>
                <ComboBox Grid.Column="1" Grid.Row="7" x:Name="QuoteComboBox" ItemTemplate="{StaticResource RStringTemplate}" SelectionChanged="ComboBox_SelectionChanged" />
                <!-- Comment -->
                <TextBlock Grid.Column="0" Grid.Row="8">Comment</TextBlock>
                <ComboBox Grid.Column="1" Grid.Row="8" x:Name="CommentComboBox" ItemTemplate="{StaticResource RStringTemplate}" SelectionChanged="ComboBox_SelectionChanged" />
                <!-- na.strings -->
                <TextBlock Grid.Column="0" Grid.Row="9">na.strings</TextBlock>
                <TextBox Grid.Column="1" Grid.Row="9" x:Name="NAStringTextBox" >NA</TextBox>
            </Grid>
            <Grid Grid.Column="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0">Input File</TextBlock>
                        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                            <TextBlock x:Name="InputFilePreview"></TextBlock>
                        </ScrollViewer>
                    </Grid>
                    <Grid Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0">Data Frame</TextBlock>
                        <DataGrid Grid.Row="1" x:Name="DataFramePreview" AutoGenerateColumns="False" CanUserAddRows="False" CanUserSortColumns="False">
                            <DataGrid.RowHeaderStyle>
                                <Style TargetType="{x:Type DataGridRowHeader}">
                                    <Setter Property="Content" Value="{Binding RowName}" />
                                </Style>
                            </DataGrid.RowHeaderStyle>
                        </DataGrid>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="CommandPreviewBox" Grid.Column="0" />
            <Button x:Name="RunButton" Content="Run" Grid.Column="1"
                    Click="RunButton_Click"/>
            <Button x:Name="CancelButton" Content="Cancel" Grid.Column="2"
                    Click="CancelButton_Click"/>
        </Grid>
    </Grid>
</ui:DialogWindow>
